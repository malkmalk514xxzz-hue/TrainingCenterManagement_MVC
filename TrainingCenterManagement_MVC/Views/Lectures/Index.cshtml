@model IEnumerable<TrainingCenterManagement_MVC.Models.Lecture>

@{
    ViewData["Title"] = "Lectures List";
}

<h1>Lectures</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Create New Lecture</a>
</p>

<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Title)</th>
            <th>@Html.DisplayNameFor(model => model.Description)</th>
            <th>@Html.DisplayNameFor(model => model.VideoUrl)</th>
            <th>@Html.DisplayNameFor(model => model.ThumbnailUrl)</th>
            <th>@Html.DisplayNameFor(model => model.LectureDate)</th>
            <th>@Html.DisplayNameFor(model => model.IsDeleted)</th>
            <th>@Html.DisplayNameFor(model => model.Course.CourseName)</th>
            <th style="width:150px;">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Title)</td>
                <td>@Html.DisplayFor(modelItem => item.Description)</td>
                <td>
                    @if (!string.IsNullOrEmpty(item.VideoUrl))
                    {
                        <a href="@item.VideoUrl" target="_blank">View Video</a>
                    }
                    else
                    {
                        <span class="text-muted">No Video</span>
                    }
                </td>
                <td>
                    @if (!string.IsNullOrEmpty(item.ThumbnailUrl))
                    {
                        <img src="@item.ThumbnailUrl" alt="Thumbnail" style="max-width:100px; max-height:60px;" />
                    }
                    else
                    {
                        <span class="text-muted">No Thumbnail</span>
                    }
                </td>
                <td>@item.LectureDate.ToString("yyyy-MM-dd")</td>
                <td>
                    @if (item.IsDeleted)
                    {
                        <span class="badge bg-danger">Deleted</span>
                    }
                    else
                    {
                        <span class="badge bg-success">Active</span>
                    }
                </td>
                <td>@Html.DisplayFor(modelItem => item.Course.CourseName)</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.LectureId" class="btn btn-sm btn-primary">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.LectureId" class="btn btn-sm btn-info ms-1">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.LectureId" class="btn btn-sm btn-danger ms-1">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
