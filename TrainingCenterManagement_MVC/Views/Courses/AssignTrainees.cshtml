@model Guid

@{
    ViewData["Title"] = "Assign Trainees";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow border-0">
                <div class="card-header bg-primary text-white text-center">
                    <h4 class="mb-0">Assign Trainees to Course</h4>
                </div>
                <div class="card-body">
                    <form asp-action="AssignTrainees" method="post">
                        <input type="hidden" name="courseId" value="@ViewBag.CourseId" />

                        <div class="mb-4">
                            <label for="traineeIds" class="form-label fw-bold">Select Trainees</label>
                            <select name="traineeId" id="traineeId" class="form-select select2" asp-items="ViewBag.TraineeId">
                                <option value="">-- اختر متدرباً --</option>
                            </select>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-success">Assign Trainees</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#traineeId').select2({
                placeholder: "اختر متدرباً",
                allowClear: true
            });
        });
    </script>
}
